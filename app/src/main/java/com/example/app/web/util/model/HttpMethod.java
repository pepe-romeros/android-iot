package com.example.app.web.util.model;

/**
 * RequestMethod is an enum that mapps all Request methods the Http Service is able to handle
 * currently.
 */
public enum HttpMethod {
    GET,
    POST,
    PUT,
    DELETE,
}
